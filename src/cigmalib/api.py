import numpy

def quadrature(shape, order):
    x = numpy.zeros((10,3))
    w = numpy.zeros((10,))
    return (x,w)

def tabulate(shape, degree, x):
    tab = numpy.zeros((10,8))
    return tab

def test(a,b):
    return 42
